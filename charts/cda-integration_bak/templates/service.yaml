apiVersion: v1
kind: Service
metadata:
  name: {{ include "cda-integration.fullname" . }}
  labels:
    {{- include "cda-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cda-integration.selectorLabels" . | nindent 4 }}
